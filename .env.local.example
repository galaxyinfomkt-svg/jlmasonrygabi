# Cloudflare Workers AI — free image generation (SD 1.5 inpainting).
# 1) Sign up free at https://dash.cloudflare.com/sign-up (no card required)
# 2) Account ID — found at https://dash.cloudflare.com (right sidebar of any zone, or Workers & Pages → Overview)
# 3) API Token — create one at https://dash.cloudflare.com/profile/api-tokens with "Workers AI" → Read permission
# Restart `npm run dev` after editing.
CF_ACCOUNT_ID=
CF_API_TOKEN=
